apiVersion: v1
kind: Service
metadata:
  name: das-app
  namespace: default
  labels:
    app: das-app
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      name: http
  selector:
    app: das-app
    